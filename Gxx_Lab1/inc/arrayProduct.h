#ifndef _ARR_PRODUCT
#define _ARR_PRODUCT

	void arrayProduct(float* a1, float* a2, int length, float* retArr);
	extern void asm_arrayProduct  ( float* a1, float* a2, int length, float* retArr);

#endif